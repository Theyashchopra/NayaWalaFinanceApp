<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andriod.financemanager">

    <application
        android:name=".Variable_app"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Share_retrieve_data"></activity>
        <activity
            android:name=".add_exp"
            android:parentActivityName=".sandwitch" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".sandwitch"
            android:label="@string/title_activity_sandwitch"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".nav_head_changer" />
    </application>


</manifest>